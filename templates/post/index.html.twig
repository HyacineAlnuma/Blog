{% extends "base.html.twig" %}

{% block body %}
	<h1>{{ post.title }}</h1>
	<h2>{{ post.chapo }}</h2>
	<p>{{ post.author }}</p>
	<p>{{ post.content }}</p>
	<p>{{ post.lastUpdateDate }}</p>
{% endblock %}
